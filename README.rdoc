= Tweet Density API

== Overview

A rake task is used to retrieve a live stream of tweets
with given terms. This is then stored in redis and a simple
JSON api used for retriving the density for a given term.

Written as a backend for a Titanium App Camp submission.